{"task": "01-lock", "with_skills": {"items": [{"item": "Uses `symfony/lock` — either a `LockFactory` obtained through dependency injection or `LockableTrait`, which wraps the same component.", "pass": true, "evidence": "src/Command/SendRemindersCommand.php: `use Symfony\\Component\\Console\\Command\\LockableTrait;` and `use LockableTrait;`"}, {"item": "Does not hand-roll mutual exclusion — no lock file written by the command itself, no database flag column, no PID check, no `flock()` call.", "pass": true, "evidence": "SendRemindersCommand.php uses `$this->lock()`/`$this->release()` from LockableTrait; no manual file/DB/PID logic present"}, {"item": "Adds the component with `composer require symfony/lock` — the skeleton does not ship it, so the recipe artifacts (`config/packages/lock.yaml`, the `LOCK_DSN` entry in `.env`) are present and `config/bundles.php` was not edited by hand.", "pass": true, "evidence": "config/packages/lock.yaml added; .env has `LOCK_DSN=flock`; symfony.lock updated with symfony/lock recipe entry; no bundles.php in diff"}, {"item": "Releases the lock, or relies on the component doing so.", "pass": true, "evidence": "SendRemindersCommand.php: `finally { $this->release(); }`"}]}, "without_skills": {"items": [
  {"item": "Uses `symfony/lock` — either a `LockFactory` obtained through dependency injection or `LockableTrait`, which wraps the same component.", "pass": true, "evidence": "src/Command/SendDeadlineRemindersCommand.php:16 `private readonly LockFactory $lockFactory` injected via constructor, used at line 27 `$this->lockFactory->createLock('deadlines:send-reminders')`"},
  {"item": "Does not hand-roll mutual exclusion — no lock file written by the command itself, no database flag column, no PID check, no `flock()` call.", "pass": true, "evidence": "src/Command/SendDeadlineRemindersCommand.php only calls LockFactory::createLock/acquire/release; no direct file I/O, PID check, or flock() call in the command or entity code"},
  {"item": "Adds the component with `composer require symfony/lock` — the skeleton does not ship it, so the recipe artifacts (`config/packages/lock.yaml`, the `LOCK_DSN` entry in `.env`) are present and `config/bundles.php` was not edited by hand.", "pass": true, "evidence": "config/packages/lock.yaml (new file), .env: `LOCK_DSN=flock`, symfony.lock adds `\"symfony/lock\": {...\"files\": [\"config/packages/lock.yaml\"]}`; no config/bundles.php hunk in diff"},
  {"item": "Releases the lock, or relies on the component doing so.", "pass": true, "evidence": "src/Command/SendDeadlineRemindersCommand.php:66-72 `finally { $lock->release(); }`"}
]}
```}
