{"task": "01-lock", "with_skills": {"items": [{"item": "Uses `symfony/lock` — either a `LockFactory` obtained through dependency injection or `LockableTrait`, which wraps the same component.", "pass": true, "evidence": "src/Command/SendRemindersCommand.php:9 `use Symfony\\Component\\Lock\\LockFactory;` and constructor-injected `private readonly LockFactory $lockFactory`"}, {"item": "Does not hand-roll mutual exclusion — no lock file written by the command itself, no database flag column, no PID check, no `flock()` call.", "pass": true, "evidence": "src/Command/SendRemindersCommand.php:29 uses `$this->lockFactory->createLock(...)->acquire()`; no manual file/DB/PID logic in the command"}, {"item": "Adds the component with `composer require symfony/lock` — the skeleton does not ship it, so the recipe artifacts (`config/packages/lock.yaml`, the `LOCK_DSN` entry in `.env`) are present and `config/bundles.php` was not edited by hand.", "pass": true, "evidence": "composer.json:26 adds `symfony/lock`; config/packages/lock.yaml created; .env adds `LOCK_DSN=flock`; symfony.lock records the recipe; no config/bundles.php diff present"}, {"item": "Releases the lock, or relies on the component doing so.", "pass": true, "evidence": "src/Command/SendRemindersCommand.php:37-41 `finally { $lock->release(); }`"}]}, "without_skills": {"items": [{"item": "Uses `symfony/lock` — either a `LockFactory` obtained through dependency injection or `LockableTrait`, which wraps the same component.", "pass": true, "evidence": "src/Command/SendRemindersCommand.php:16-19 constructor injects `private readonly LockFactory $lockFactory`, used at line 30 `$this->lockFactory->createLock('app:send-reminders')`"}, {"item": "Does not hand-roll mutual exclusion — no lock file written by the command itself, no database flag column, no PID check, no `flock()` call.", "pass": true, "evidence": "SendRemindersCommand.php only calls `$lock->acquire()`/`$lock->release()` via LockFactory; no direct `flock()`, PID file, or DB flag column in the diff"}, {"item": "Adds the component with `composer require symfony/lock` — the skeleton does not ship it, so the recipe artifacts (`config/packages/lock.yaml`, the `LOCK_DSN` entry in `.env`) are present and `config/bundles.php` was not edited by hand.", "pass": true, "evidence": "config/packages/lock.yaml created (framework.lock: '%env(LOCK_DSN)%'), .env adds LOCK_DSN=flock block, symfony.lock records the symfony/lock recipe; config/bundles.php not touched in diff"}, {"item": "Releases the lock, or relies on the component doing so.", "pass": true, "evidence": "src/Command/SendRemindersCommand.php:70-72 `finally { $lock->release(); }`"}]}}
