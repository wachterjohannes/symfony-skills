{"task": "01-lock", "with_skills": {"items": [
  {"item": "Uses `symfony/lock` — either a `LockFactory` obtained through dependency injection or `LockableTrait`, which wraps the same component.", "pass": true, "evidence": "src/Command/SendRemindersCommand.php:9,17 — `use Symfony\\Component\\Console\\Command\\LockableTrait;` and `use LockableTrait;`"},
  {"item": "Does not hand-roll mutual exclusion — no lock file written by the command itself, no database flag column, no PID check, no `flock()` call.", "pass": true, "evidence": "src/Command/SendRemindersCommand.php — only calls `$this->lock()`/`$this->release()` from LockableTrait; no flock()/file/PID logic; Deadline.remindedAt is reminder-tracking, not a mutex flag"},
  {"item": "Adds the component with `composer require symfony/lock` — the skeleton does not ship it, so the recipe artifacts (`config/packages/lock.yaml`, the `LOCK_DSN` entry in `.env`) are present and `config/bundles.php` was not edited by hand.", "pass": true, "evidence": "composer.json:26 adds symfony/lock; config/packages/lock.yaml (new); .env:49-54 LOCK_DSN block; symfony.lock:137-148 recipe entry; no config/bundles.php hunk in diff"},
  {"item": "Releases the lock, or relies on the component doing so.", "pass": true, "evidence": "src/Command/SendRemindersCommand.php:69-71 — `finally { $this->release(); }`"}
]}
```, "without_skills": {"items": [{"item": "Uses `symfony/lock` — either a `LockFactory` obtained through dependency injection or `LockableTrait`, which wraps the same component.", "pass": true, "evidence": "src/Command/SendRemindersCommand.php:16 `private readonly LockFactory $lockFactory` injected via constructor; used at line 26 `$this->lockFactory->createLock('app:send-reminders', ttl: self::LOCK_TTL_SECONDS)`"}, {"item": "Does not hand-roll mutual exclusion — no lock file written by the command itself, no database flag column, no PID check, no `flock()` call.", "pass": false, "evidence": "src/Repository/DeadlineRepository.php:38-46 `claimReminder()` does an atomic `UPDATE deadline SET reminder_sent_at = :now WHERE id = :id AND reminder_sent_at IS NULL` — a hand-rolled compare-and-swap mutex using a database flag column (`reminder_sent_at`), on top of the symfony/lock usage"}, {"item": "Adds the component with `composer require symfony/lock` — the skeleton does not ship it, so the recipe artifacts (`config/packages/lock.yaml`, the `LOCK_DSN` entry in `.env`) are present and `config/bundles.php` was not edited by hand.", "pass": true, "evidence": "composer.json:+\"symfony/lock\": \"8.1.*\"; config/packages/lock.yaml (new file) with `framework: lock: '%env(LOCK_DSN)%'`; .env:+LOCK_DSN=\"postgresql+advisory://...\"; symfony.lock updated with symfony/lock recipe entry; config/bundles.php not touched in diff"}, {"item": "Releases the lock, or relies on the component doing so.", "pass": true, "evidence": "src/Command/SendRemindersCommand.php:33-37 `try { $this->sendReminders($output); } finally { $lock->release(); }`"}]}}
