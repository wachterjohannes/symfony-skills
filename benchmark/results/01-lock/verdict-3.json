{"task": "01-lock", "with_skills": {"items": [{"item": "Uses `symfony/lock` — either a `LockFactory` obtained through dependency injection or `LockableTrait`, which wraps the same component.", "pass": true, "evidence": "src/Command/SendDeadlineRemindersCommand.php: `private readonly LockFactory $lockFactory` injected in constructor, used as `$this->lockFactory->createLock('app:send-deadline-reminders')`"}, {"item": "Does not hand-roll mutual exclusion — no lock file written by the command itself, no database flag column, no PID check, no `flock()` call.", "pass": true, "evidence": "src/Command/SendDeadlineRemindersCommand.php uses only `$lock->acquire()`/`$lock->release()` from LockFactory; no direct flock()/file/DB-flag logic in the command"}, {"item": "Adds the component with `composer require symfony/lock` — the skeleton does not ship it, so the recipe artifacts (`config/packages/lock.yaml`, the `LOCK_DSN` entry in `.env`) are present and `config/bundles.php` was not edited by hand.", "pass": true, "evidence": "composer.json adds \"symfony/lock\": \"8.1.*\"; config/packages/lock.yaml created; .env has `LOCK_DSN=flock`; symfony.lock records the symfony/lock recipe; no config/bundles.php hunk in diff"}, {"item": "Releases the lock, or relies on the component doing so.", "pass": true, "evidence": "src/Command/SendDeadlineRemindersCommand.php: `try { $sent = ... } finally { $lock->release(); }`"}]}, "without_skills": {"items": [{"item": "Uses `symfony/lock` — either a `LockFactory` obtained through dependency injection or `LockableTrait`, which wraps the same component.", "pass": true, "evidence": "src/Command/SendDeadlineRemindersCommand.php:24 `private readonly LockFactory $lockFactory,` injected via constructor, used at line 47 `$this->lockFactory->createLock('app:send-deadline-reminders');`"}, {"item": "Does not hand-roll mutual exclusion — no lock file written by the command itself, no database flag column, no PID check, no `flock()` call.", "pass": true, "evidence": "src/Command/SendDeadlineRemindersCommand.php:47-51 uses `$this->lockFactory->createLock(...)->acquire()`; no direct file/PID/flock() handling in the command"}, {"item": "Adds the component with `composer require symfony/lock` — the skeleton does not ship it, so the recipe artifacts (`config/packages/lock.yaml`, the `LOCK_DSN` entry in `.env`) are present and `config/bundles.php` was not edited by hand.", "pass": true, "evidence": "composer.json:26 adds `\"symfony/lock\": \"8.1.*\"`; config/packages/lock.yaml created; .env adds `LOCK_DSN=flock` block; symfony.lock records the symfony/lock recipe; config/bundles.php not touched in diff"}, {"item": "Releases the lock, or relies on the component doing so.", "pass": true, "evidence": "src/Command/SendDeadlineRemindersCommand.php:88-90 `finally { $lock->release(); }`"}]}}
