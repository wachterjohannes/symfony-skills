{"task": "02-json-endpoint", "with_skills": {"items": [{"item": "Reads the body with `#[MapRequestPayload]` on the action argument, not `json_decode()` on the request content.", "pass": true, "evidence": "src/Controller/SubscribeController.php:14 — `public function __invoke(#[MapRequestPayload] SubscribeRequest $request): JsonResponse`"}, {"item": "Validates with `symfony/validator` constraints on a DTO (`#[Assert\\Email]`, `#[Assert\\Choice]`), not with hand-written `if` checks.", "pass": true, "evidence": "src/Dto/SubscribeRequest.php:8-15 — `#[Assert\\Email]` on `$email` and `#[Assert\\Choice(choices: ['basic', 'pro'])]` on `$plan`, no if-checks present"}, {"item": "Returns 422 for invalid input without writing the error-handling code by hand — the framework produces it from the failed validation.", "pass": false, "evidence": "No error-handling or exception-listener code is shown in the diff; only the success path `return $this->json(['status' => 'subscribed'], 201);` is present, so the 422 behavior cannot be confirmed from the diff"}]}, "without_skills": {"items": [{"item": "Reads the body with `#[MapRequestPayload]` on the action argument, not `json_decode()` on the request content.", "pass": false, "evidence": "src/Controller/SubscribeController.php:14 — `$data = json_decode($request->getContent(), true);`"}, {"item": "Validates with `symfony/validator` constraints on a DTO (`#[Assert\\Email]`, `#[Assert\\Choice]`), not with hand-written `if` checks.", "pass": true, "evidence": "src/Dto/SubscribeRequest.php:10-15 — `#[Assert\\Email]` and `#[Assert\\Choice(choices: ['basic', 'pro'])]` on DTO properties, validated via `$validator->validate($subscribeRequest)`"}, {"item": "Returns 422 for invalid input without writing the error-handling code by hand — the framework produces it from the failed validation.", "pass": false, "evidence": "src/Controller/SubscribeController.php:24-34 — hand-written foreach over `$violations` building the error array and manually returning `new JsonResponse(['errors' => $errors], 422)`"}]}}
